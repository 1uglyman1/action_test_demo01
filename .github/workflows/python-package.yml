# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python package

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11"]

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0 #必须启动，否则可能会导致结果无法回传提交
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: '3.8'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
    - name: Run test
      run: |
        python test.py
    - name: Run test_2file
      run: |
        mkdir -p result  
        python -c "open('result/output.txt', 'w').write('Data generated at $(date)')"  
    - name: Commite2Back
      run: |
          git config --global user.name "1uglyman1"  
          git config --global user.email "2173869002@qq.com"  
          git add result/  
          git commit -m "Update result [auto]"  
          git push 
      env:  
          GITHUB_TOKEN: ${{ secrets._1UGLYMAN1 }}  # 直接引用即可  
